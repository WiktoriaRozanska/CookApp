import 'package:flutter/material.dart';

class RecepiesScreen extends StatelessWidget {
  static const routeName = '/recepies';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Main page"),
      ),
      // bottomNavigationBar: ,
    );
  }
}
